import { AppHeader } from "../components/AppHeader";

export default function PanelEstudiante() {
  return (
    <div>
      <AppHeader right={null} />
      <p className="ls-body text-center mt-10">Panel del estudiante — en construcción (Fase 5).</p>
    </div>
  );
}
