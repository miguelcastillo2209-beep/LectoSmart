# URL del backend desplegado (ej. https://lectosmart-backend.onrender.com).
# En desarrollo local se deja vacía/sin definir: el proxy de Vite
# (vite.config.js) redirige /api al backend en localhost:4000.
VITE_API_URL=""
